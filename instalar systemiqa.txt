::::::::::::::::::::	
	INSTALAR SYSTEMIQA
::::::::::::::::::::

1. Ubicarse en la carpeta del proyecto SystemIQA
  1.1 Renombrar el archivo "env.example" por "env" de la carpeta del proyecto SystemIQA
  1.2 Configurar el archivo "env" con nombre y las credenciales de la base.

2. $composer install

2.55 $composer dump-autoload

2.555 $ php artisan db:sedd

2.5 $php artisan config:cache

3. $php artisan key:generate

4.
 
IF (Ya hay una base "systemiqa" en MySQL)
	$php artisan migrate:refresh
ELSE
	$php artisan migrate


5. $composer update

6. $php artisan serve

abc123